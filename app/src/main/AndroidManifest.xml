<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.ufrpe.brunna.adminnomades">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".estabelecimento.gui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pessoa.gui.PerfilActivity" />
        <activity android:name=".pessoa.gui.EditarPerfilActivity" />
        <activity android:name=".pessoa.gui.PerfisListActivity" />
        <activity android:name=".pessoa.gui.MainActivity" />
        <activity android:name=".pessoa.gui.CadastrarPerfilActivity"></activity>
    </application>

</manifest>